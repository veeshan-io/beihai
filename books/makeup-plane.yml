---
# - name: Update servers with common setting
#   import_playbook: makeup-common.yml

- name: Update plane servers
  hosts: plane
  remote_user: root
  vars:
  roles:
    - role: grace-init
